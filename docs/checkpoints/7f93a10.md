# Guided forms checkpoint: 7f93a10

Model/tool revision: `7f93a109741e5a2e056fbb322efc9ac8383a4940`.
Previous code checkpoint this session: `a2b2e0646a0a065aad0b4ccaff657424358608b0`.
Active model: `cyber-form-study` — Prism / guided form refinement.
Dedicated workflow: `35029181995`, Guided form and detail study, successful.
Artifact: `guided-form-review`, ID `10420608104`.

## Tested evidence

All **165 local repository tests passed**. The local site build passed with 15 discovered recipes. The expanded targeted set has **31 tests** and also passed independently in the dedicated workflow. The full local form review and the CI form review completed successfully. All four scene variants and three standalone hair GLBs validated with zero errors and warnings. The standalone boot was additionally validated locally, also with zero errors and warnings; its CI review rendered and exported it but did not run that extra validator call. Informational unused-UV messages remain in the full reports.

The hero PNG, baked scene GLB, baked hair GLB, visible-hair mask and alignment JSON were compared with the downloaded CI artifact and are byte-identical for these runs. This is not a cross-browser guarantee. Hero SHA-256: `05dc4d543f23dd88396b487b9a277c733de85a26e8a29490f133858204753f52`. Scene GLB SHA-256: `8bbaea2dbe2619e2b7bea67135994a377e656c72ea86c94d41022374ac2a364a`.

The scene with low/baked hair has **519,152 triangles**. It preserves the named camera, five lights, embedded maps and small Survey clip. The clean scene export before and after preview posing is byte-identical. No full-body skin rig or native Blender render is newly verified here.

## What actually improved

The prior pose-only baseline remains unchanged. The current scene uses the recovered rail-based armor, shell layers and guided hair, plus this session's shared upper hair support, boundary constraints, primary-surface cache, reduced jaw/nose relief and lofted boot soles. No new anatomical template, tutorial footage or image-generated geometry is used.

The nine manually annotated feature locations retain **9.357 px RMS** projection error at 768 x 1376. These are authoring observations, not held-out data. The coarse convex hair envelope improves from **0.576 to 0.928** against the unchanged annotation. More importantly, a separate rendered visible-hair mask, including occlusion by other geometry and the annotated face opening, improves from **0.480 to 0.802 IoU**. Neither number is a likeness percentage. The raster annotation is approximate, with twelve-pixel uncertainty, and was not changed to fit this model.

Both shared upper-hair support boundaries have sampled position mismatch below `1e-10 m` (129 samples each). This is not a welded render-mesh guarantee: triangulation, tangent frames and high-only relief are separate. The crown and side shading still expose folds.

The 192 x 192 primary-support cache was independently compared at 2,048 non-grid points. RMS position error is **0.002626 mm**, 95th percentile **0.005022 mm**, maximum **0.036035 mm**, below the chosen 0.05 mm bound for this support. A coarser trial failed this bound and was not adopted. This cache is an approximation, distinct from the lossless attribute-indexing utility.

## Hair geometry and normal transfer

Hair only: high **138,304 triangles**; low and baked **15,424 triangles**, an **88.8478% reduction**. Two 512 x 512 normal maps are embedded in the baked variant. Low and baked positions, indices, normals, UVs and tangents are exactly equal in the tests.

The independent area-weighted normal test uses 4,096 samples per chart and bilinear level-zero lookup:

| Chart | Low mean / p95 / max | Baked mean / p95 / max |
| --- | --- | --- |
| Curtain | 0.550 / 1.918 / 36.799 degrees | 0.197 / 0.362 / 2.258 degrees |
| Fringe | 1.354 / 3.867 / 47.038 degrees | 0.271 / 0.613 / 8.768 degrees |

Both charts require zero UV-edge extension samples. The **fringe maximum still fails the five-degree editorial target** and is retained in `remainingTargets`. These tests are rest-pose normal-transfer diagnostics, not silhouette, mipmap, posed tangent, material, collision or likeness certification. The complete character is not a low-poly bake.

## Visual inspection and next step

Actual hero, neutral side/back, head with/without hair, and boot renders were opened. The hair outline is closer, but its crown remains angular with an unattractive groove. The face is still doll-like and the eye/lip surfaces are not connected facial topology. Armor remains broad and simplistic; some rear fins and trim overlap; the boots have a better sole but still a regular toe bumper and tall open cuff. The luminous loop is still opaque and too bright. Matching a silhouette does not solve these primary 3D forms.

Read `docs/guided-forms.md`. Next work should establish smoother primary head/hair cross-sections and better chest/shoulder contours, holding the reference, pose and camera fixed and comparing alternative views. Do not add pores or more decorative meshes to hide those defects. The old pose and original scene recipes are retained. This successful component workflow does not establish that the separate all-gallery Pages deployment succeeded.
