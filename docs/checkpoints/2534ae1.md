# Verified reference-pose checkpoint

Model/tool revision: `2534ae1190d7fb0d073463ef581b0368c82c0c3f`.
Model: `cyber-pose-study` (Prism / pose before detail).
Dedicated workflow: `35016314254`, Reference pose and composition study, completed successfully.
Artifact: `reference-pose-review` (`10415896464`).

The local full repository suite passed **133 tests**, and `npm run build` succeeded with 14 discovered recipes. The dedicated workflow independently passed 16 targeted projection, segment-length, socket, annotation, overlay and batch-lifecycle tests. All four study GLBs validated with zero errors and warnings. Informational unused-UV messages remain in the full, untruncated reports.

The study rendered 12 locked-camera images across gesture, masses, assembly and the Survey head-pose test. Separate hero, baseline and side/back clay captures bring the review to 16 views/passes. The before/after recipes deliberately have different cameras: correcting composition is part of this experiment. Only the four construction stages are camera/light locked to each other.

The complete scene has **511,544 triangles**, one authored camera and five lights. The simple gesture scene has 70,320 triangles and the volume scene 73,584, including their shared platform and city; these are not character-only counts. The assembly GLB is byte-identical before and after preview posing.

The downloaded CI hero PNG, assembly GLB, gesture GLB and alignment JSON were compared against the local files and were byte-identical for this revision. In particular:

- Hero PNG SHA-256: `db46c872dd0ad7678de626953f942c2a7e7ec35c329ded6c63326ad4312c0d71`.
- Assembly GLB SHA-256: `41863de4097c75d0b6c9d9cc36d0f812b42f8e280236933e9264fd1357133bfa`.

Nine manually annotated feature points have RMS projection error **166.680 pixels before** and **9.357 pixels after**, with maximum remaining error **17.959 pixels**, at 768 x 1376 reference coordinates. These observations were used during modeling, not held out. This is neither a likeness score nor a measure of recovered 3D anatomy.

The coarse convex hair-envelope overlap is **0.576**, still below the editorial target of 0.85. The automated report therefore keeps `nextPriorities: ["hair mass envelope"]` and `visualAcceptance: "not-assessed"`. The reference overlay also shows the full remaining envelope mismatch. A local graduated-cut trial barely changed that score and was rejected, despite a better bounding rectangle.

The actual images still show a helmet-like bob, doll-like face, simplified armor and slab-like boots. The side clay exposes approximate depths, broad rear armor and stretched component proportions. Matching projected joints plus equal limb lengths is useful, but it does not certify contact, correct anatomy, absence of intersections, or a good pose from every viewpoint.

This pass has no new high-to-low normal bake, skinned humanoid, native Blender validation or complete animation rig. Main-loop sockets resolve at build time; arbitrary moving mechanisms need a subsequent constraint/animation implementation. The earlier scene is retained as a baseline.

Read `docs/reference-pose.md`. Resume with one failed mass/silhouette region at a time under the fixed hero camera; inspect side/back views too. Do not hide errors by changing the reference annotations or increasing decorative density. The successful dedicated study does not imply that the separate full-gallery Pages deployment has completed.
