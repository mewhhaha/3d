# Verified component checkpoint: 30ecf15

Code revision: `30ecf15caa1fa1649413eabac8938edb6de0d25f`.
Component workflow: `34965477753` (Anatomy component studies), completed successfully.

## Verified outputs

The component tests passed: 36 tests. The browser rendered and exported hand, forearm, eye and arm in sculpt, cage and baked modes. All 12 GLBs passed the validator with zero errors and were re-imported for structural checks. The arm GLBs each retain 19 `NODE_SKINNED_MESH_NON_ROOT` hierarchy warnings; the reports are not warning-free.

The arm has 275,734 high-detail triangles and 17,204 low-detail triangles, a 93.7607% reduction. Cage and baked versions have identical positions, normals, UVs, tangents and skin weights. This is corresponding chart resampling, not arbitrary decimation.

The arm's Blender 4.0.2 report records 19 bones, 19 mesh objects with UVs, eight normal-map nodes/images, and ElbowFlex, ForearmTurn, Grasp and WristFlex actions. Imported elbow rotation moved evaluated vertices by about 0.245 m and the finger test by about 0.031 m; restoration error was zero in the recorded test. The packed lit scene was saved, reopened and rendered.

## Do not confuse functional success with visual quality

The rendered palm/finger junction remains visibly simplified and can show a seam. The arm's silhouette, thumb/nail shaping and skin material are not a convincing reproduction of the explorer image. The shoulder is a study cut; the old whole-character recipe has not been replaced.

The independent 2,048-sample, bilinear, level-zero normal test reports a palm mean error reduction from about 1.546 to 0.144 degrees. The knuckle-web mean improves from about 6.384 to 1.049 degrees, but its worst sampled error is still about 80.854 degrees. That large outlier is a failed local fidelity target, not a lossless bake. Inspect the full mean/95th-percentile/maximum report rather than quoting the average alone.

The default 256-pixel chart textures are the verified asset configuration. An additional exploratory 512-pixel knuckle-web bake hit the strict 0.004 UV-edge tolerance. Increasing texture resolution alone is therefore not a generally reliable path at this junction. The shared boundary parameterization and branching topology need further work; do not advertise arbitrary texture resolutions as verified.

## Resume from here

Read `docs/arm-study.md` and `docs/junction-study.md`. Focus on a continuous palm/finger/web cage and shared boundary correspondence, rather than adding more pores or masking the seam with lighting. The low/high silhouettes, normals and posed geometry need separate acceptance checks. A successful Blender import and closed-edge audit do not establish anatomical likeness, absence of intersections or production deformation quality.
